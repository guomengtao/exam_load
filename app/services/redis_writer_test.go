package services


// 测试 Redis 导入器的启动、运行和写入逻辑（避免误删 Hash，仅操作 ZSet 成员）
// 注意：此测试重点验证 ZSet 成员的处理，避免误删 Hash 数据，确保测试环境安全
// 已删除 TestStartRedisImporter 函数，避免与 redis_importer_test.go 冲突

// (TestStartRedisImporter 函数已移除)